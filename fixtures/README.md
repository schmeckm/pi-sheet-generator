# Fixtures

Sample data for manual tests and scripts (not used in production).

| File | Use |
|------|-----|
| `test-xsteps.csv` | Repository import smoke test |
| `images/pi-test.png` | `server/scripts/test-vision-api.js` |
